FROM mcr.microsoft.com/mssql/server:latest

# Create a config directory
RUN mkdir -p /usr/config
WORKDIR /usr/config

# Bundle config source
COPY --chown=mssql:mssql config/ /usr/config

ENTRYPOINT ["./entrypoint.sh"]